# Architecture

**Canonical documentation is the repository root [`README.md`](../README.md).**

That file defines stack, request flow (`proxy.ts` → headers → App Router), entry/exit points, directory map, rendering boundaries, content pipeline, env vars, security, CI, and content authoring rules.

This file exists so links to `docs/architecture.md` still resolve. Do not add a second system map or duplicated tables here—extend **`README.md`** instead.
